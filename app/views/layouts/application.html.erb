<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:fb="http://www.facebook.com/2008.fbml">
<head>
  <title>HumanConnections</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>

<div class="user">
  <%= link_to "Logout", :logout unless !logged_in? %>
  <%= link_to "Home", :home unless !logged_in? %>
  <%= link_to "Accounts", :accounts unless !logged_in? %>
</div>

<% [:error, :success, :notice].each do |key| %>
  <% if flash[key] %>
    
    <br />
    
    <div class="<%= key %>" id="flash_<%= key %>">
      <%= flash[key] %>
    </div>
  <% end %>
<% end %>

<%= yield %>

</body>
</html>
